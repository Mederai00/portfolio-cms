{% extends 'base.html.twig' %}

{% block title %}Edit Section
{% endblock %}

{% block body %}
	{% include 'admin-nav.html.twig' %}
	<section class="ftco-section bg-light">
		<div class="container">
			<h1>Edit Section</h1>

			{{ include('section/_form.html.twig', {'button_label': 'Update'}) }}

			{{ include('section/_delete_form.html.twig') }}


			<a href="{{ path('section_index') }}" class=" btn btn-outline btn-warning mt-2">back to list</a>

		</div>
	</section>
{% endblock %}
