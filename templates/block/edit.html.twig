{% extends 'base.html.twig' %}

{% block title %}Edit Block
{% endblock %}

{% block body %}
	<section class="ftco-section bg-light" id="skills-section">
		<div class="container">
			<h1>Edit Block</h1>

			{{ include('block/_form.html.twig', {'button_label': 'Update'}) }}

			<a href="{{ path('block_index') }}" class=" btn btn-outline btn-warning mt-2">back to list</a>

			{{ include('block/_delete_form.html.twig') }}
		</div>
	</section>
{% endblock %}
